import { warn } from '../utils/log.js';

export default async function publish(_args) {
  warn('nww-forge publish is not yet implemented.');
  warn('Planned: upload make output to GitHub Releases via gh CLI.');
  process.exit(1);
}
